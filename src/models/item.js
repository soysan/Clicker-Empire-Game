export default class Item {
  constructor(name, type, maxVal, desc, price, effect) {
    this.name = name;
    this.type = type;
    this.maxVal = maxVal;
    this.desc = desc;
    this.price = price;
    this.effect = effect;
  }
}
